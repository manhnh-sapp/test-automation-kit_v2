# Compiled class files
*.class

# Package Files
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# Logs
*.log
logs/
Log/

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# IntelliJ IDEA
.idea/
*.iml
*.iws
out/

# Eclipse
.project
.classpath
.settings/
bin/

# VS Code
.vscode/

# Maven
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
# Avoid ignoring the Maven wrapper jar
!/.mvn/wrapper/maven-wrapper.jar

# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Selenium / Appium logs
chromedriver.log
geckodriver.log
appium.log

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Reports (TestNG, Allure, ExtentReports)
test-output/
allure-results/
allure-report/
ExtentReports/
reports/
Reports/
ExportData/

# Metrics/reliability telemetry (F10/F11) — Persistence Option A: file TỔNG HỢP được COMMIT
# (versioned learning store, cùng triết lý knowledge/). Chỉ CI merge-report (scheduled, single job)
# commit-back nên không nhiễu local. File tạm/khác trong dir vẫn ignore.
knowledge/metrics/*
!knowledge/metrics/.gitkeep
!knowledge/metrics/runs.jsonl
!knowledge/metrics/tc-history.jsonl
!knowledge/metrics/reliability-index.json
!knowledge/metrics/reliability-index.md
!knowledge/metrics/quarantine.json

# Agent / AI local files
.gemini/
brain/
artifacts/
.agent/logs/
.agent/tmp/
.agent/config/.assignee_cache.json

# Temp data/files
.playwright-mcp/
.selenium-mcp/
.appium-mcp/

# Environment & credentials
.env
.env.local
.env.*.local
.env.bak*
**/.env
**/.env.local
**/.env.*.local
**/.env.bak*
.agent/config/*.local.md

# Per-project run profiles (chứa credential riêng từng dự án) — không commit
profiles/**/task.env

# Node modules
node_modules/

# GitLab Runner config (chứa token — KHÔNG commit; runner đặt ở C:\GitLab-Runner)
config.toml

# Office temp/lock files
~$*.xlsx
~$*.xls
~$*.docx

# Google Service Account (ignore both naming conventions)
service_account.json
service-account.json

# Google OAuth (kit login) — client secret + token, KHÔNG commit
**/oauth-credentials.json
**/token.json

# One-off operational backups (chứa accountId/tên nội bộ)
scripts/integrations/jira/unassign_tests_backup.json
**/*_backup.json
**/*.BACKUP.md

# Claude Code local files
.claude/
CLAUDE.md

# Task outputs — dữ liệu chạy per-task, KHÔNG commit; chỉ giữ khung outputs/
/outputs/*
!/outputs/.gitkeep

# Báo cáo AI Agent (deck pptx/pdf) — bản trình bày, KHÔNG commit vào kit
/AI-Agent-Activity-Report.*

# Tài liệu kế hoạch/đánh giá/hướng dẫn nội bộ (plan/review/guide) — KHÔNG commit (lộ roadmap/điểm review lên public)
*_KeHoach_va_HuongDan*.docx
*_KeHoach*.docx

# GitLab Runner system id
.runner_system_id
